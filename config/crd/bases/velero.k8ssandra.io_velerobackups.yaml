---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.8.0
  creationTimestamp: null
  name: velerobackups.velero.k8ssandra.io
spec:
  group: velero.k8ssandra.io
  names:
    kind: VeleroBackup
    listKind: VeleroBackupList
    plural: velerobackups
    singular: velerobackup
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VeleroBackup is the Schema for the cassandrabackups API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: VeleroBackupSpec defines the desired state of VeleroBackup
            properties:
              datacenters:
                items:
                  type: string
                type: array
              k8ssandraCluster:
                description: LocalObjectReference contains enough information to let
                  you locate the referenced object inside the same namespace.
                properties:
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      TODO: Add other useful fields. apiVersion, kind, uid?'
                    type: string
                type: object
            required:
            - k8ssandraCluster
            type: object
          status:
            description: VeleroBackupStatus defines the observed state of VeleroBackup
            properties:
              datacenters:
                additionalProperties:
                  properties:
                    phase:
                      type: string
                  required:
                  - phase
                  type: object
                type: object
              finishTime:
                format: date-time
                type: string
              startTime:
                format: date-time
                type: string
            required:
            - datacenters
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
